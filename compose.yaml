services:
  nginx:
    image: nginx:latest
    ports:
      - 80:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./templates:/etc/nginx/html:ro
      - ./static:/etc/nginx/static:ro
    restart: always
